{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",
  "name": "${mod_name}",
  "description": "${mod_description}",
  "authors": [
    "${mod_authors}"
  ],
  "contributors": [
    "${mod_credits}"
  ],
  "contact": {
    "sources": "${mod_source}",
    "issues": "${mod_issues}"
  },
  "license": "${mod_license}",
  "icon": "logo.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "pizzart.listings.LegacyListings::init"
    ],
    "client": [
      "pizzart.listings.LegacyListingsClient::init"
    ]
  },
  "depends": {
    "fabric-api": "*",
    "factory_api": ">=${factory_api_version}",
    "minecraft": ["${mc_version_range}"]
  },
  "suggests": {
    "factocrafty": ">=0.0.0.6"
  }
}